{
  "name": "careerpilotai-root",
  "version": "1.0.0",
  "description": "Root workspace for CareerPilot AI",
  "scripts": {
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "concurrently \"npm run start --prefix backend\" \"npm run start --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
